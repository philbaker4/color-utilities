
interface DataGradientStep {
    minVal: number;
    maxVal: number;
    color: string | number[];
}

interface GradientDefinition {
    minVal: number;
    minColor: string;
    maxVal: number;
    maxColor: string;
}